export enum expressTypes {
  SURFACE = "surface",
  AIR = "air",
  SEA = "sea",
}

export enum courierCode {
  ZERO = 0,
  ONE = 1,
}

export enum paymentMethods {
  COD = "COD",
  PREPAID = "PREPAID",
}

export enum addressType {
  HOME = "HOME",
  WORK = "WORK",
  OTHER = "OTHER",
}

export enum orderStatus {
  PENDING = "pending",
  CANCELLED = "cancelled",
  COMPLETED = "completed",
  RETURNED = "returned",
}
