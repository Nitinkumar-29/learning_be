export class ParcelXRequestDocumentRepository {
    // log request
    async create(requestLog: any): Promise<void> {
        // Implement the logic to save the request log to your database
        // For example, you can use a MongoDB collection to store the logs
        // await db.collection('parcelx_request_logs').insertOne(requestLog);
    }
}